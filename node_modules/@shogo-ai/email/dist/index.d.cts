import { E as EmailTemplate } from './types-CCHryvHe.cjs';
export { a as EmailAddress, b as EmailConfig, c as EmailError, d as EmailErrorCode, e as EmailProviderType, f as EmailResult, I as IEmailProvider, g as IEmailService, O as OciEmailConfig, S as SendEmailParams, h as SendTemplateParams, i as SesConfig, j as SmtpConfig, T as TemplateRegistry, k as formatEmailAddress } from './types-CCHryvHe.cjs';

/**
 * Email Template System
 *
 * Simple template system using {{variable}} interpolation.
 * No dependencies - just string replacement.
 */

/**
 * Interpolate variables into a template string.
 *
 * Replaces {{variableName}} with the corresponding value from data.
 * Supports nested access: {{user.name}} → data.user.name
 *
 * @param template - Template string with {{variable}} placeholders
 * @param data - Data object with values to interpolate
 * @param options - When `escapeValues` is true, interpolated values are
 *   HTML-encoded (use for HTML bodies; leave off for plain-text subject/text)
 * @returns Interpolated string
 */
declare function interpolate(template: string, data: Record<string, unknown>, options?: {
    escapeValues?: boolean;
}): string;
/**
 * Strip HTML tags to create plain text version.
 *
 * @param html - HTML string
 * @returns Plain text string
 */
declare function htmlToText(html: string): string;
/**
 * Template registry class for managing email templates.
 */
declare class EmailTemplateRegistry {
    private templates;
    /**
     * Register a template
     */
    register<TData extends Record<string, unknown>>(template: EmailTemplate<TData>): void;
    /**
     * Get a template by name
     */
    get(name: string): EmailTemplate | undefined;
    /**
     * Check if a template exists
     */
    has(name: string): boolean;
    /**
     * Render a template with data
     */
    render<TData extends Record<string, unknown>>(name: string, data: TData): {
        subject: string;
        html: string;
        text: string;
    };
    /**
     * List all registered template names
     */
    list(): string[];
}
/**
 * Built-in welcome email template
 */
declare const welcomeTemplate: EmailTemplate<{
    name: string;
    appName: string;
    loginUrl?: string;
}>;
/**
 * Built-in password reset email template
 */
declare const passwordResetTemplate: EmailTemplate<{
    name?: string;
    appName: string;
    resetUrl: string;
    expiresIn?: string;
}>;
/**
 * Built-in invitation email template
 */
declare const invitationTemplate: EmailTemplate<{
    inviterName: string;
    resourceName: string;
    resourceType?: string;
    role?: string;
    acceptUrl: string;
    appName: string;
}>;
/**
 * Built-in notification email template
 */
declare const notificationTemplate: EmailTemplate<{
    title: string;
    message: string;
    actionUrl?: string;
    actionText?: string;
    appName: string;
}>;
/**
 * All built-in templates
 */
declare const builtinTemplates: EmailTemplate[];
/**
 * Create a template registry with built-in templates pre-registered
 */
declare function createTemplateRegistry(): EmailTemplateRegistry;

/**
 * Shared Shogo email layout.
 * All templates wrap their content with this layout for consistent branding.
 * Uses <style> block with CSS classes — no inline styles.
 */
declare const EMAIL_CONSTANTS: {
    readonly APP_NAME: "Shogo";
    readonly APP_URL: "https://shogo.ai";
    readonly SUPPORT_EMAIL: "support@shogo.ai";
    readonly LOGO_URL: "https://shogo.ai/assets/images/shogo-logo-email.png";
    readonly LOGO_DARK_URL: "https://shogo.ai/assets/images/shogo-logo-dark-email.png";
    readonly FOOTER_TEXT: "&copy; {{currentYear}} Shogo. All rights reserved";
};
/** Dark overrides without media query — used by the preview tool to force dark mode. */
declare const DARK_STYLE_OVERRIDES: string;
declare function wrapInLayout(content: string): string;

/**
 * Shogo Email Templates
 *
 * All Shogo-branded transactional email templates.
 * Uses <style> blocks with CSS classes — no inline styles.
 */

declare const allTemplates: EmailTemplate[];
/**
 * Create a template registry pre-loaded with all Shogo templates.
 */
declare function createShogoTemplateRegistry(): EmailTemplateRegistry;

export { DARK_STYLE_OVERRIDES, EMAIL_CONSTANTS, EmailTemplate, EmailTemplateRegistry, builtinTemplates, createShogoTemplateRegistry, createTemplateRegistry, htmlToText, interpolate, invitationTemplate, notificationTemplate, passwordResetTemplate, allTemplates as shogoTemplates, welcomeTemplate, wrapInLayout };
