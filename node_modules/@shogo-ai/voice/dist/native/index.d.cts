import * as React from 'react';
import { B as BaseVoiceConversationOptions, a as BaseVoiceConversationResult, b as BaseChatConversationOptions, c as BaseChatConversationResult, O as OrganicSphereConfig, d as OrganicParticlesConfig } from '../organicParticlesShaders-DIva0vBO.cjs';
export { e as BandReactivity, C as ChatClientToolFn, f as ChatConversationStatus, g as ChatToolDescriptor, D as DEFAULT_ORGANIC_PARTICLES_CONFIG, h as DEFAULT_ORGANIC_SPHERE_CONFIG, i as ORGANIC_PARTICLES_FRAGMENT_SHADER, j as ORGANIC_PARTICLES_VERTEX_SHADER, k as ORGANIC_SPHERE_FRAGMENT_SHADER, l as ORGANIC_SPHERE_VERTEX_SHADER, P as PERLIN_4D, r as resolveOrganicParticlesConfig, m as resolveOrganicSphereConfig } from '../organicParticlesShaders-DIva0vBO.cjs';
import * as react_jsx_runtime from 'react/jsx-runtime';
import { StyleProp, ViewStyle } from 'react-native';
import 'ai';

/**
 * `ShogoVoiceProvider` (native) — root provider that every
 * `useShogoVoice` / `useVoiceConversation` caller from
 * `@shogo-ai/sdk/voice/native` needs to live under.
 *
 * Mirrors the web provider in [packages/sdk/src/voice/react/ShogoVoiceProvider.tsx]
 * but wraps `@elevenlabs/react-native`'s `ConversationProvider` instead
 * of `@elevenlabs/react`'s. Importantly, just importing
 * `@elevenlabs/react-native` runs its module side-effects:
 *
 *   - `registerGlobals()` from `@livekit/react-native` polyfills the
 *     WebRTC globals the underlying ElevenLabs client expects.
 *   - `setSetupStrategy(reactNativeSessionSetup)` swaps the voice
 *     session bootstrapper to start a native iOS/Android AudioSession
 *     before opening the LiveKit connection.
 *
 * That's why this provider is the single recommended import path on
 * native — pulling in `@elevenlabs/react-native` somewhere near the
 * root of the app is what makes the native voice path work at all.
 */

interface ShogoVoiceProviderProps {
    children: React.ReactNode;
    /**
     * Optional default agent id forwarded to `ConversationProvider`.
     * Omit if your app passes `signedUrl` per-session via
     * `useVoiceConversation` (the recommended Shogo flow).
     */
    agentId?: string;
}
declare function ShogoVoiceProvider(props: ShogoVoiceProviderProps): React.ReactElement;

/**
 * `useVoiceConversation` (native) — drop-in React Native hook wrapping
 * `@elevenlabs/react-native`'s `useConversation` with the same battery-
 * included behaviour as the web hook (signed-URL fetch, `add_memory`
 * tool, contextual memory injection, transcript persistence).
 *
 * Differences from the web implementation:
 *
 *   - Imports from `@elevenlabs/react-native` rather than
 *     `@elevenlabs/react`. The RN package re-exports the same
 *     `useConversation` API and additionally configures WebRTC +
 *     native AudioSession internally, so the wrapper logic is
 *     identical.
 *   - There is no `navigator.mediaDevices.getUserMedia` pre-call —
 *     LiveKit handles mic permission negotiation when the session
 *     opens. Consumers can pass an optional `requestPermissions`
 *     callback (typically wired to `expo-av` or
 *     `react-native-permissions`) which is awaited before the
 *     signed-URL fetch.
 *   - The browser's `pagehide` + `navigator.sendBeacon` flush is
 *     replaced with an `AppState.addEventListener('change', ...)`
 *     handler that fires a regular `fetch` POST when the app moves
 *     to the background. There's no native equivalent to `sendBeacon`,
 *     but the OS keeps the JS context alive long enough during a
 *     background transition for the request to leave the device.
 *
 * Requires the host app to install `@elevenlabs/react-native`,
 * `@livekit/react-native`, and `@livekit/react-native-webrtc`. All
 * three are optional peer dependencies of `@shogo-ai/sdk`.
 */

interface UseVoiceConversationOptions extends BaseVoiceConversationOptions {
    /**
     * Optional pre-flight microphone permission hook. Called once
     * before each `start()` (and the `start()` triggered inside
     * `restart()`). Throw to abort the session — the rejection
     * propagates out through `start()` so the consumer can present a
     * permissions UI.
     *
     * Typical wiring with `expo-av`:
     *
     * ```ts
     * import * as Audio from 'expo-av'
     * useVoiceConversation({
     *   characterName: 'Shogo',
     *   requestPermissions: async () => {
     *     const { status } = await Audio.requestPermissionsAsync()
     *     if (status !== 'granted') {
     *       throw new Error('Microphone permission denied')
     *     }
     *   },
     * })
     * ```
     *
     * If omitted, the SDK relies on LiveKit's internal permission flow
     * (which presents the OS prompt the first time the audio track
     * starts). Wiring this explicitly gives consumers a deterministic
     * place to surface a custom denial UI.
     */
    requestPermissions?: () => Promise<void> | void;
}
type UseVoiceConversationResult = BaseVoiceConversationResult;
/**
 * Native hook that owns the voice session lifecycle end-to-end. See
 * the module docblock for the wire format of the various server
 * endpoints.
 */
declare function useVoiceConversation(options: UseVoiceConversationOptions): UseVoiceConversationResult;

/**
 * `useShogoVoice` (native) — zero-config voice hook for Expo / React
 * Native pod apps.
 *
 * Mirrors the web counterpart in
 * [packages/sdk/src/voice/react/useShogoVoice.ts] — same options, same
 * result shape — so a pod that already drives the web sphere can swap
 * its import path from `@shogo-ai/sdk/voice/react` to
 * `@shogo-ai/sdk/voice/native` and keep the rest of its code unchanged.
 *
 * Defaults applied here:
 *
 *   - `characterName: 'Shogo'`
 *   - `signedUrlPath: '/api/voice/signed-url'`
 *   - `fetchCredentials: 'include'` — RN apps are always cross-origin
 *     to the Shogo API, so `same-origin` (the web default) makes no
 *     sense; `include` carries any cookies the consumer's auth flow
 *     stored in the native cookie jar.
 *
 * Provider requirement
 * --------------------
 * Mount `<ShogoVoiceProvider>` near the root of your app (it wraps
 * `@elevenlabs/react-native`'s `ConversationProvider`). Without it
 * the underlying `useRegisterCallbacks` throws:
 *
 *     useRegisterCallbacks must be used within a ConversationProvider
 *
 * Just importing `@elevenlabs/react-native` (which `ShogoVoiceProvider`
 * does for you) is also what wires up the WebRTC polyfills and the
 * native AudioSession setup strategy, so don't lazy-import the
 * provider — keep it in the tree from app start.
 *
 * @example
 * ```tsx
 * import {
 *   ShogoVoiceProvider,
 *   useShogoVoice,
 * } from '@shogo-ai/sdk/voice/native'
 *
 * function VoiceButton() {
 *   const { start, end, status } = useShogoVoice()
 *   const active = status === 'connected' || status === 'connecting'
 *   return (
 *     <Pressable onPress={active ? end : start}>
 *       <Text>{active ? 'End call' : 'Talk to Shogo'}</Text>
 *     </Pressable>
 *   )
 * }
 *
 * export default function App() {
 *   return (
 *     <ShogoVoiceProvider>
 *       <VoiceButton />
 *     </ShogoVoiceProvider>
 *   )
 * }
 * ```
 *
 * Named secondary agents
 * ----------------------
 * Same as on web: declare extra agents in `shogo.config.json#agents`,
 * run `bunx shogo deploy`, then pass `agentName` to drive a specific
 * persona. `useShogoVoice({ agentName })` and `useShogoChat({
 * agentName })` resolve to the same `ProjectAgent` row.
 *
 * Per-user dynamic variables
 * --------------------------
 * Pass `dynamicVariables: { user_display_name, relationship_stage }`
 * (or any keys the agent prompt references) to surface per-user
 * fields from your local `Companion` row to ElevenLabs at session
 * start. The SDK's built-ins (`character_name`, `user_context`,
 * `conversation_id`) always win on collision.
 */

type UseShogoVoiceOptions = Partial<UseVoiceConversationOptions>;
declare function useShogoVoice(opts?: UseShogoVoiceOptions): UseVoiceConversationResult;

/**
 * `useChatConversation` (native) — text-only sibling of
 * {@link useVoiceConversation}.
 *
 * Mirrors the web counterpart in
 * [packages/sdk/src/voice/react/useChatConversation.ts] — same options,
 * same result shape — so a pod that already drives the web chat panel
 * can swap its import path from `@shogo-ai/sdk/voice/react` to
 * `@shogo-ai/sdk/voice/native` and keep the rest of its code unchanged.
 *
 * Differences from the web implementation:
 *
 *   - Default `fetchCredentials` flips from `'same-origin'` (web) to
 *     `'omit'` when `shogoApiKey` is set, or `'include'` otherwise.
 *     RN apps are always cross-origin to the backend, so `same-origin`
 *     makes no sense; `include` carries any cookies the consumer's
 *     auth flow stored in the native cookie jar.
 *   - No DOM hooks. `@ai-sdk/react` is platform-agnostic; the import
 *     surface used here (`useChat`, `DefaultChatTransport`) runs the
 *     same way on RN/Hermes/JSC.
 *
 * Requires the host app to install `@ai-sdk/react` and `ai`. Both are
 * optional peer deps of `@shogo-ai/sdk`.
 *
 * @experimental V1 surface — shape may evolve before promotion.
 */

type UseChatConversationOptions = BaseChatConversationOptions;
type UseChatConversationResult = BaseChatConversationResult;
declare function useChatConversation(options?: UseChatConversationOptions): UseChatConversationResult;

/**
 * `useShogoChat` (native) — zero-config text-chat hook for Expo / React
 * Native pod apps.
 *
 * Mirrors the web counterpart in
 * [packages/sdk/src/voice/react/useShogoChat.ts] — same options, same
 * result shape — so a pod that already drives the web chat panel can
 * swap its import path from `@shogo-ai/sdk/voice/react` to
 * `@shogo-ai/sdk/voice/native` and keep the rest of its code unchanged.
 *
 * Defaults applied here:
 *
 *   - `api: '/api/chat/turn'`
 *   - `fetchCredentials: 'include'` — RN apps are always cross-origin
 *     to the Shogo API, so `same-origin` (the web default) makes no
 *     sense; `include` carries any cookies the consumer's auth flow
 *     stored in the native cookie jar.
 *
 * For external SDK consumers using a Shogo API key, drive
 * `useChatConversation({ shogoApiKey, projectId, ... })` directly —
 * the bearer path defaults to `'omit'` automatically.
 *
 * @experimental V1 surface — shape may evolve before promotion.
 */

type UseShogoChatOptions = Partial<UseChatConversationOptions>;
declare function useShogoChat(opts?: UseShogoChatOptions): UseChatConversationResult;

interface OrganicSphereProps {
    /**
     * Returns the current agent-output frequency buffer (length = fftSize,
     * values 0–255). Typically `useVoiceConversation().getOutputByteFrequencyData`.
     */
    getFrequencyData?: () => Uint8Array | null;
    /**
     * Whether audio is live. When `false` the sphere settles into its
     * idle pose (no pulsing). Purely cosmetic — the component keeps the
     * render loop running either way.
     *
     * @default true
     */
    active?: boolean;
    /**
     * Full configuration object (partial). Any unspecified keys fall back
     * to `DEFAULT_ORGANIC_SPHERE_CONFIG`.
     */
    config?: Partial<OrganicSphereConfig>;
    /** Inline style for the wrapping `<GLView>`. Size the parent — the GLView fills it. */
    style?: StyleProp<ViewStyle>;
}
/**
 * Audio-reactive organic sphere for React Native (Expo). See the
 * module docblock for usage.
 */
declare function OrganicSphere({ getFrequencyData, active, config: configProp, style, }: OrganicSphereProps): react_jsx_runtime.JSX.Element;

interface OrganicParticlesProps {
    getFrequencyData?: () => Uint8Array | null;
    active?: boolean;
    config?: Partial<OrganicParticlesConfig>;
    style?: StyleProp<ViewStyle>;
}
/**
 * Audio-reactive particle cloud for React Native (Expo). See the
 * module docblock for usage.
 */
declare function OrganicParticles({ getFrequencyData, active, config: configProp, style, }: OrganicParticlesProps): react_jsx_runtime.JSX.Element;

export { BaseChatConversationOptions, BaseChatConversationResult, OrganicParticles, OrganicParticlesConfig, type OrganicParticlesProps, OrganicSphere, OrganicSphereConfig, type OrganicSphereProps, ShogoVoiceProvider, type ShogoVoiceProviderProps, type UseChatConversationOptions, type UseChatConversationResult, type UseShogoChatOptions, type UseShogoVoiceOptions, type UseVoiceConversationOptions, type UseVoiceConversationResult, useChatConversation, useShogoChat, useShogoVoice, useVoiceConversation };
