import {
  createVoiceHandlers
} from "./chunk-O362EJQR.js";

// src/route/index.ts
function defaultHandlers() {
  return createVoiceHandlers({});
}
function createVoiceRoute(config) {
  const v = createVoiceHandlers(config);
  return {
    signedUrl: { GET: (req) => v.signedUrl(req) },
    ttsPreview: { POST: (req) => v.tts(req) },
    music: { POST: (req) => v.music(req) },
    agent: {
      POST: (req) => v.agent.create(req),
      PATCH: (req) => v.agent.patch(req),
      DELETE: (req) => v.agent.delete(req)
    },
    audioTags: { GET: (req) => v.audioTags(req) }
  };
}

export {
  defaultHandlers,
  createVoiceRoute
};
//# sourceMappingURL=chunk-T3T4EBWY.js.map