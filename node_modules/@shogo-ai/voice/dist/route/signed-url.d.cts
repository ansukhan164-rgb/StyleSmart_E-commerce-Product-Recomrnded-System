/**
 * Drop-in `GET /voice/signed-url` handler.
 *
 * @example
 * ```ts
 * // app/api/voice/signed-url+api.ts (Expo Router)
 * export { GET } from '@shogo-ai/sdk/voice/route/signed-url'
 * ```
 */
declare const GET: (req: Request) => Promise<Response>;

export { GET };
